using UnityEngine;
using System.Collections;

public class EnemyManager : MonoBehaviour {
    //TODO Hold info of every two enemy patrol edges to respawn enemy once the enemy deviate the edges
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
